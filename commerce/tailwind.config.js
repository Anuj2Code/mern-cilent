module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  theme: {
    extend: {
      screens: {
        
      },
    },
  },
  plugins: [],
}
// - The `min-*` and `max-*` variants are not supported with a `screens` configuration containing mixed units.